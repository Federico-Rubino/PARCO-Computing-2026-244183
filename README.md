□ Compiler version and flags
□ How to compile and run (local and cluster) with example commands
□ Brief explanation of input and output
□ Description of what we need to modify to change the parameters (matrix sizes,
number of threads/CPUs) and default values
□ Cluster-specific notes (modules, queues)
